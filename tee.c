#include<stdio.h>

int main()
{
		int a,b,c,g;
			
		printf("输入商品价格: ");

				scanf("%d %d %d\n; &a &b &c");
						
						printf("%d+%d=%d\n", a, b, c);
								

							if(c>=200) g=0.8*c;
									
								else

										if(c>=300) g=0.6*c;
									
									else
											
											if(c<200) g=c;

									printf("折后价格%d\n,g");
}#include<stdio.h>

int main()
{
		int a,b,c,g;
			
		printf("输入商品价格: ");

				scanf("%d %d %d\n; &a &b &c");
						
						printf("%d+%d=%d\n", a, b, c);
								

							if(c>=200) g=0.8*c;
									
								else

										if(c>=300) g=0.6*c;
									
									else
											
											if(c<200) g=c;

									printf("折后价格%d\n,g");
}
